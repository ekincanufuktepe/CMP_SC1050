#include<stdio.h>

int main() {

    // You cannot use keywords for variable and function names
    int if = 5; // bad
    int int = 2; // bad
    // Also, your variable names cannot start with a number
    int 1_variable = 1; // bad

    return 0;
}