#include<stdio.h>

int main() {

    ;
    int sum = 0;
    // Test the difference between for() and for();
    for(int num=2; num <= 100; sum+=num, num+=2);
    
    printf(" ");
        
    printf("sum: %d\n", sum);
    return 0;
}