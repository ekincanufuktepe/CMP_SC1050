#include<stdio.h>

int main() {

    // infinite loop example
    while(-1) { // non-zero values are true, and the value zero is false.
        printf("I will run forever!!!\n");
    }

    return 0;
}