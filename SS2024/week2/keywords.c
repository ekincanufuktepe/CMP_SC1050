#include<stdio.h>

int main() {
    // int 1int = 0; // this is an invalid variable name, they cannot start with a number
    // printf("%d\n", 1int); 
    int grade = 7;
    // use " for strings
    // use ' for characters (char)

    printf("Status: %d\n", grade >= 60 ? 1 : 0);

    return 0;
}